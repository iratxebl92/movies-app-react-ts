import React from "react"


export const Reviews = () => {

    return (
        <div>Reviews</div>
    )
}

// React.memo funciona SOLO cuando hay props. Evita que el componente se renderice de nuevo si no han cambiado las props entre renderizados 